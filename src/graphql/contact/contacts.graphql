query GetContactList(
  $limit: Int
  $offset: Int
  $order_by: [contact_order_by!]
) {
  contact(limit: $limit, offset: $offset, order_by: $order_by) {
    created_at
    first_name
    id
    last_name
    phones {
      number
    }
  }
}
